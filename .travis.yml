sudo: required
services:
    - docker

before_install:
    - docker build -t ckgh96/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run ckgh96/react-test npm test -- --coverage

after_success:
    - docker build -t ckgh96/multi-client ./client
    - docker build -t ckgh96/multi-nginx ./nginx
    - docker build -t ckgh96/multi-server ./server
    - docker build -t ckgh96/multi-worker ./worker
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    - docker push ckgh96/multi-client
    - docker push ckgh96/multi-nginx
    - docker push ckgh96/multi-server
    - docker push ckgh96/multi-worker


